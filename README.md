# A Wild Question Game (AWQG)

Versiones publicadas:
- Windows (web zip)
- Android (APK)

## Build Android
1. `npm install`
2. `npx cap sync android`
3. `cd android && gradlew.bat assembleDebug`

APK generado en:
`android/app/build/outputs/apk/debug/app-debug.apk`
